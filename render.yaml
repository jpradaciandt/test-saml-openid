services:
  - type: web
    name: sample-openid
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/sample-openid-0.0.1-SNAPSHOT.jar
    envVars:
      - key: ISSUER_URL
        value: https://YOUR-APP-NAME.onrender.com
      - key: TOKEN_USERNAME
        value: admin
      - key: TOKEN_PASSWORD
        sync: false   # Set this as a secret in the Render dashboard
    plan: free
